# Project context (quick reference)

**Broadcast Box work**: Branch `broadcast-box` is ahead by 5 commits. Current
state, completed tasks, key docs, and next steps are in:

**→
[docs/broadcast-box/BROADCAST-BOX-PROJECT-STATE.md](../docs/broadcast-box/BROADCAST-BOX-PROJECT-STATE.md)**

That doc includes:

- Recent commits (error handling, RTMP streaming, sources.set)
- Summary of completed work (source config, RTMP, structured errors)
- Key docs (Complete Int, DEVICE-MESSAGE-PROTOCOL,
  ERROR-HANDLING-IMPLEMENTATION-PLAN)
- Code locations (errors, command-handlers, device-command-service, API, UI)
- Known issues (failing DB integration test, device firmware)
- Suggested next steps (fix tests, push, firmware)

Use it to restore context when continuing Broadcast Box or related work.
